# O frontend le o .env da raiz do monorepo.
# Este arquivo existe apenas como referencia das variaveis que o Next consome.
NEXT_PUBLIC_API_URL=http://localhost:3333/api/v1
NEXT_PUBLIC_APP_NAME=Sinapse
